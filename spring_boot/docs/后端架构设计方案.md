# 仓库管理系统(WMS)后端架构设计方案

## 文档信息

- **版本**: 1.0
- **创建时间**: 2026-02-08
- **作者**: 架构设计团队
- **状态**: 设计阶段

---

## 一、前端项目分析总结

### 1.1 技术栈分析

#### 前端技术栈
- **核心框架**: Vue 3.4.21 (Composition API)
- **构建工具**: Vite 6.3.0
- **UI组件库**: Element Plus 2.6.3
- **状态管理**: Pinia 2.1.7
- **路由管理**: Vue Router 4.3.0
- **HTTP客户端**: Axios 1.11.0
- **工具库**: ExcelJS 4.4.0, Crypto-JS 4.2.0, ECharts 6.0.0

### 1.2 核心业务模块

#### 资产管理模块
- 设备台账
- 设备列表
- 设备类型管理
- 设备状态审批

#### 库存管理模块
- 入库管理
- 出库管理
- 库存调拨
- 库存盘点
- 库存状态监控
- 库存预警
- 批次管理
- 货位管理
- 仓库管理

#### 业务记录模块
- 安装管理
- 维修管理
- 保养管理
- 报废管理
- 库存审计

#### 查询统计模块
- 综合查询
- 数据报表
- 数据分析

#### 系统管理模块
- 用户管理
- 角色管理
- 权限管理
- 区域管理
- 系统配置
- 操作日志

### 1.3 API接口设计规范

#### 路径映射规则
- **前端路径**: 不包含`/api`前缀 (如: `/devices/list`)
- **Vite代理**: 自动添加`/api`前缀 (如: `/api/devices/list`)
- **后端context-path**: `/api`
- **后端Controller**: `@RequestMapping("/devices")` + `@GetMapping("/list")`

#### 统一响应格式
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {},
  "success": true
}
```

#### 分页响应格式
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "records": [],
    "total": 100,
    "current": 1,
    "size": 10
  }
}
```

### 1.4 数据模型分析

#### 设备状态枚举
```java
public enum DeviceStatus {
    PENDING_INBOUND(-1, "待入库"),
    IN_STOCK(0, "在库"),
    IN_USE(1, "使用中"),
    INSTALLED(2, "已安装"),
    MAINTENANCE(3, "维护中"),
    REPAIRING(4, "维修中"),
    SCRAPPED(5, "已报废");
}
```

#### 用户角色枚举
```java
public enum UserRole {
    ADMIN(0, "管理员"),
    OPERATOR(1, "操作员"),
    VIEWER(2, "查看员");
}
```

---

## 二、后端系统架构设计

### 2.1 整体架构

#### 架构模式
- **分层架构**: Controller → Service → Repository → Database
- **前后端分离**: RESTful API
- **微服务准备**: 模块化设计,易于拆分

#### 技术栈选择
- **核心框架**: Spring Boot 3.5.5
- **Java版本**: JDK 17
- **安全框架**: Spring Security 6.1.0 + JWT
- **ORM框架**: Spring Data JPA 3.1.0
- **数据库**: MySQL 8.0 (生产) / H2 (开发测试)
- **缓存**: Redis 7.0
- **API文档**: SpringDoc OpenAPI (Swagger)
- **日志框架**: SLF4J + Logback
- **工具库**: Lombok, MapStruct, Hutool

### 2.2 分层架构设计

```
com.backend.wms/
├── controller/              # 控制器层
│   ├── auth/               # 认证控制器
│   ├── device/             # 设备管理控制器
│   ├── inventory/          # 库存管理控制器
│   ├── system/             # 系统管理控制器
│   └── ...
├── service/                 # 业务逻辑层
│   ├── impl/               # 服务实现
│   ├── AuthService.java
│   ├── DeviceService.java
│   ├── InventoryService.java
│   └── ...
├── repository/              # 数据访问层
│   ├── UserRepository.java
│   ├── DeviceRepository.java
│   ├── InventoryRepository.java
│   └── ...
├── entity/                  # 实体类
│   ├── User.java
│   ├── Device.java
│   ├── Inventory.java
│   └── ...
├── dto/                     # 数据传输对象
│   ├── request/            # 请求DTO
│   ├── response/           # 响应DTO
│   └── ...
├── config/                  # 配置类
│   ├── SecurityConfig.java
│   ├── JwtConfig.java
│   ├── RedisConfig.java
│   └── ...
├── exception/               # 异常处理
│   ├── GlobalExceptionHandler.java
│   ├── BusinessException.java
│   └── ...
├── util/                    # 工具类
│   ├── JwtUtil.java
│   ├── RedisUtil.java
│   └── ...
├── enums/                   # 枚举类
│   ├── DeviceStatus.java
│   ├── UserRole.java
│   └── ...
├── constants/               # 常量类
│   ├── ApiPathConstants.java
│   ├── RedisKeyConstants.java
│   └── ...
└── WmsApplication.java      # 启动类
```

---

## 三、数据库模型设计

### 3.1 核心实体设计

#### 3.1.1 用户表 (sys_user)
```sql
CREATE TABLE sys_user (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '用户ID',
    username VARCHAR(50) NOT NULL UNIQUE COMMENT '用户名',
    password VARCHAR(255) NOT NULL COMMENT '密码(BCrypt加密)',
    real_name VARCHAR(50) NOT NULL COMMENT '真实姓名',
    email VARCHAR(100) NOT NULL COMMENT '邮箱',
    phone VARCHAR(20) NOT NULL COMMENT '手机号',
    avatar VARCHAR(255) COMMENT '头像URL',
    status TINYINT NOT NULL DEFAULT 1 COMMENT '状态(0:禁用,1:启用)',
    deleted TINYINT NOT NULL DEFAULT 0 COMMENT '删除标记(0:未删除,1:已删除)',
    created_by BIGINT COMMENT '创建人ID',
    created_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_by BIGINT COMMENT '更新人ID',
    updated_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX idx_username (username),
    INDEX idx_email (email),
    INDEX idx_phone (phone),
    INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';
```

#### 3.1.2 设备表 (device)
```sql
CREATE TABLE device (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '设备ID',
    device_code VARCHAR(50) NOT NULL UNIQUE COMMENT '设备编号',
    device_name VARCHAR(100) NOT NULL COMMENT '设备名称',
    device_type_id BIGINT NOT NULL COMMENT '设备类型ID',
    serial_number VARCHAR(100) COMMENT '序列号',
    model VARCHAR(100) COMMENT '型号',
    manufacturer VARCHAR(100) COMMENT '制造商',
    purchase_date DATE COMMENT '采购日期',
    purchase_price DECIMAL(10,2) COMMENT '采购价格',
    status INT NOT NULL DEFAULT 0 COMMENT '设备状态(-1:待入库,0:在库,1:使用中,2:已安装,3:维护中,4:维修中,5:已报废)',
    running_status VARCHAR(20) COMMENT '运行状态(running,standby,fault,offline)',
    warehouse_id BIGINT COMMENT '所属仓库ID',
    bin_id BIGINT COMMENT '所属货位ID',
    area_id BIGINT COMMENT '所属区域ID',
    city_id BIGINT COMMENT '城市ID',
    district_id BIGINT COMMENT '区县ID',
    location_id BIGINT COMMENT '位置ID',
    specifications JSON COMMENT '规格参数(JSON格式)',
    remote_account JSON COMMENT '远程账户信息(JSON格式)',
    attachment_id BIGINT COMMENT '附件ID',
    remark VARCHAR(500) COMMENT '备注',
    created_by BIGINT COMMENT '创建人ID',
    created_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_by BIGINT COMMENT '更新人ID',
    updated_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX idx_device_code (device_code),
    INDEX idx_device_type_id (device_type_id),
    INDEX idx_status (status),
    INDEX idx_warehouse_id (warehouse_id),
    INDEX idx_bin_id (bin_id),
    INDEX idx_area_id (area_id),
    INDEX idx_serial_number (serial_number),
    FOREIGN KEY (device_type_id) REFERENCES device_type(id),
    FOREIGN KEY (warehouse_id) REFERENCES warehouse(id),
    FOREIGN KEY (bin_id) REFERENCES bin(id),
    FOREIGN KEY (area_id) REFERENCES area(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='设备表';
```

---

## 四、API接口规范设计

### 4.1 API路径规范

#### 4.1.1 路径常量定义
```java
public class ApiPathConstants {
    
    public static final String API_PREFIX = "/api";
    
    public static class AuthApi {
        public static final String BASE = API_PREFIX + "/auth";
        public static final String LOGIN = BASE + "/login";
        public static final String LOGOUT = BASE + "/logout";
        public static final String INFO = BASE + "/info";
        public static final String REFRESH_TOKEN = BASE + "/refresh";
    }
    
    public static class DeviceApi {
        public static final String BASE = API_PREFIX + "/devices";
        public static final String LIST = BASE + "/list";
        public static final String DETAIL = BASE + "/{id}";
        public static final String CREATE = BASE;
        public static final String UPDATE = BASE + "/{id}";
        public static final String DELETE = BASE + "/{id}";
        public static final String BATCH_DELETE = BASE + "/batch";
        public static final String IMPORT = BASE + "/import";
        public static final String EXPORT = BASE + "/export";
        public static final String STATISTICS = BASE + "/statistics";
    }
}
```

### 4.2 统一响应格式

#### 4.2.1 成功响应
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {},
  "success": true
}
```

#### 4.2.2 分页响应
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "records": [],
    "total": 100,
    "current": 1,
    "size": 10
  }
}
```

---

## 五、安全策略设计

### 5.1 Spring Security配置

#### 5.1.1 安全配置类
```java
@Configuration
@EnableWebSecurity
@EnableMethodSecurity
@RequiredArgsConstructor
public class SecurityConfig {
    
    private final JwtAuthenticationFilter jwtAuthenticationFilter;
    private final AuthenticationProvider authenticationProvider;
    
    @Bean
    public SecurityFilterChain securityFilterChain(HttpSecurity http) throws Exception {
        http
            .csrf(AbstractHttpConfigurer::disable)
            .cors(Customizer.withDefaults())
            .sessionManagement(session -> session
                .sessionCreationPolicy(SessionCreationPolicy.STATELESS)
            )
            .authorizeHttpRequests(auth -> auth
                .requestMatchers("/api/auth/**").permitAll()
                .requestMatchers("/api/performance-report").permitAll()
                .requestMatchers("/api/error-report").permitAll()
                .requestMatchers("/api/swagger-ui/**", "/api/v3/api-docs/**").permitAll()
                .anyRequest().authenticated()
            )
            .authenticationProvider(authenticationProvider)
            .addFilterBefore(jwtAuthenticationFilter, UsernamePasswordAuthenticationFilter.class);
        
        return http.build();
    }
}
```

### 5.2 JWT配置

#### 5.2.1 JWT工具类
```java
@Component
@Slf4j
public class JwtService {
    
    @Value("${jwt.secret}")
    private String secret;
    
    @Value("${jwt.expiration}")
    private Long jwtExpiration;
    
    @Value("${jwt.refresh-token.expiration}")
    private Long refreshExpiration;
    
    public String generateToken(UserDetails userDetails) {
        return buildToken(new HashMap<>(), userDetails, jwtExpiration);
    }
    
    public String generateRefreshToken(UserDetails userDetails) {
        return buildToken(new HashMap<>(), userDetails, refreshExpiration);
    }
    
    private String buildToken(
        Map<String, Object> extraClaims,
        UserDetails userDetails,
        Long expiration
    ) {
        return Jwts
            .builder()
            .setClaims(extraClaims)
            .setSubject(userDetails.getUsername())
            .setIssuedAt(new Date(System.currentTimeMillis()))
            .setExpiration(new Date(System.currentTimeMillis() + expiration))
            .signWith(getSignInKey(), SignatureAlgorithm.HS256)
            .compact();
    }
}
```

---

## 六、性能优化方案

### 6.1 数据库优化

#### 6.1.1 索引优化
- 为常用查询字段创建索引
- 使用复合索引优化多条件查询
- 定期分析慢查询并优化

#### 6.1.2 查询优化
- 使用分页查询避免全表扫描
- 使用JOIN替代子查询
- 避免SELECT *,只查询需要的字段

### 6.2 缓存优化

#### 6.2.1 Redis缓存策略
```java
@Cacheable(value = "devices", key = "#id")
public Device getDeviceById(Long id) {
    return deviceRepository.findById(id).orElse(null);
}

@CacheEvict(value = "devices", key = "#device.id")
public Device updateDevice(Device device) {
    return deviceRepository.save(device);
}
```

---

## 七、开发里程碑和实施计划

### 7.1 开发阶段划分

#### 第一阶段: 基础架构搭建 (1-2周)
- 项目初始化和依赖配置
- 数据库设计和建表
- 基础架构搭建(Controller、Service、Repository)
- 统一响应格式和异常处理
- Spring Security和JWT集成
- API文档配置(Swagger)

#### 第二阶段: 认证和权限模块 (1周)
- 用户管理模块
- 角色管理模块
- 权限管理模块
- 登录认证接口
- Token刷新机制

#### 第三阶段: 设备管理模块 (2周)
- 设备类型管理
- 设备基本信息管理
- 设备状态管理
- 设备状态转换规则
- 设备状态审批流程
- 设备状态跟踪

#### 第四阶段: 库存管理模块 (2周)
- 仓库管理
- 货位管理
- 批次管理
- 入库管理
- 出库管理
- 库存调拨
- 库存盘点
- 库存审计

#### 第五阶段: 业务记录模块 (1周)
- 安装管理
- 维修管理
- 报废管理
- 业务记录查询

#### 第六阶段: 系统管理模块 (1周)
- 区域管理
- 系统配置
- 操作日志
- 数据报表

#### 第七阶段: 性能优化和测试 (1周)
- 性能优化
- 单元测试
- 集成测试
- 前后端联调

#### 第八阶段: 部署和上线 (1周)
- 生产环境部署
- 数据库迁移
- 系统监控
- 文档完善

### 7.2 技术风险和应对措施

#### 风险1: 前后端数据格式不一致
**应对措施**: 
- 建立统一的数据格式规范
- 使用DTO进行数据转换
- 前后端联调时充分测试

#### 风险2: 性能问题
**应对措施**:
- 使用Redis缓存热点数据
- 数据库索引优化
- 分页查询避免大数据量
- 异步处理耗时操作

#### 风险3: 安全问题
**应对措施**:
- 使用JWT认证
- 密码BCrypt加密
- SQL注入防护
- XSS攻击防护

---

## 八、总结

本架构设计方案基于对前端项目的全面分析,结合仓库管理系统的业务特点,设计了一套完整的后端解决方案。方案包括:

1. **系统架构**: 采用分层架构,前后端分离,RESTful API设计
2. **数据库设计**: 设计了24张核心表,覆盖所有业务场景
3. **API接口**: 统一的接口规范,与前端完全对接
4. **安全策略**: Spring Security + JWT,完整的权限控制
5. **性能优化**: 数据库优化、Redis缓存、异步处理等
6. **开发计划**: 8个开发阶段,预计8-10周完成

该方案能够确保前后端无缝对接,满足业务需求,并具备良好的可扩展性和可维护性。
